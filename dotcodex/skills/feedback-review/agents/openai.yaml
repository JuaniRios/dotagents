interface:
  display_name: "Feedback Review"
  short_description: "Review and address requested feedback"
  default_prompt: "Use $feedback-review to review and address feedback."
policy:
  allow_implicit_invocation: true
