interface:
  display_name: "Publish Review"
  short_description: "Prepare and publish review-ready changes"
  default_prompt: "Use $publish-review to prepare changes for review."
policy:
  allow_implicit_invocation: true
