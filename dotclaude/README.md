# dotclaude

Claude Code global skills and slash commands inside the `dotagents` repo.

- `commands/` — symlinked to `~/.claude/commands`
- `skills/` — symlinked to `~/.claude/skills`

Codex-native versions live in `../dotcodex/skills/`.
