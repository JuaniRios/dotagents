# dotclaude

Claude Code global skills inside the `dotagents` repo. Each skill is a folder
with a `SKILL.md`; it is both a `/<name>` slash command and something Claude
can invoke on its own.

- `skills/` — child-linked into `~/.claude/skills`

Codex-native versions live in `../dotcodex/skills/`.
